<%= form_for(@article, html: { multipart: true }) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.file_field :picture, accept: 'image/jpeg, image/gif, image/png' %>

  <%= f.submit "Submit" %>
<% end %>